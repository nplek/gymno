import { Company } from './company';

export class Department {
    depId: number;
    name: string;
    shortName: string;
    company: Company;
    active: string;
    createdAt: string;
    updatedAt: string;
}